const EXTERNAL_API_URI = 'https://echo-serv.tbxnet.com/v1/secret'
// This should be in a .env file but per challenge instructions, It's hardcoded here
const EXTERNAL_API_TOKEN = 'Bearer aSuperSecretKey'

module.exports = {
  EXTERNAL_API_URI,
  EXTERNAL_API_TOKEN
}
